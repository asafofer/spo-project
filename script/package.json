{
  "name": "prebid-collector-script",
  "version": "1.0.0",
  "module": "src/collector.js",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun run --watch build.ts",
    "serve": "bun run build.ts && bun run serve.ts",
    "version:patch": "bun run version.ts patch",
    "version:minor": "bun run version.ts minor",
    "version:major": "bun run version.ts major",
    "version": "bun run version.ts",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@types/bun": "latest",
    "happy-dom": "^15.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
