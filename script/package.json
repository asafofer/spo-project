{
  "name": "prebid-collector-script",
  "version": "1.0.0",
  "module": "src/collector.js",
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "watch": "bun run --watch scripts/build.ts",
    "serve": "bun run scripts/serve.ts",
    "version:patch": "bun run scripts/version.ts patch",
    "version:minor": "bun run scripts/version.ts minor",
    "version:major": "bun run scripts/version.ts major",
    "version": "bun run scripts/version.ts",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@types/bun": "latest",
    "happy-dom": "^15.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
